Workflow skill: $synthia-workflow 
(/home/dan/.codex/skills/synthia-workflow/SKILL.md)

## Codex Execution Directive

You are acting as the Synthia development agent.

Your job is to implement tasks listed in this file sequentially.

Rules:

1. Work through tasks in numerical order.
2. Continue executing tasks until ALL tasks are completed.
3. Do not stop after partial implementation.
4. Do not return summaries unless every task is finished.
5. If a task cannot be completed:
   - document the blocker
   - implement everything else that is possible
   - continue to the next task.

Execution behavior:

- Read the repository before modifying code.
- Follow existing architecture and coding conventions.
- Update or create files as required.
- Run tests or validation where possible.
- Commit after each completed task.

Completion criteria:

A task is considered complete only when:
- implementation exists in the codebase
- it compiles or runs without breaking existing features
- any required documentation is updated

Persistence rule:

Continue working autonomously until all tasks in this document are completed.
Do not stop early.

if there is missing data for the task check in the @Core-Documents folder.for core documentation
if it is not present and should be core-owned create a ticket for the req. data in the core dodc
if it is not present and should be node-owned create a file with the new data in the core docs

Do not edit above this line:
#######################################################################
Got it. Then skip the compatibility dance and go straight to a **hard cutover plan**.

That means Phase 2 becomes:

* rename MQTT root
* rename helpers
* rename docs/examples
* rename notifications/bootstrap/topics
* update all producers/consumers/nodes
* no legacy `synthia/...` support kept

Here’s the copy-paste-ready Phase 2.

````md
# Phase 2 Tasks — Full Namespace Migration to Hexe
# Start at Task 640

## Scope

This phase performs a **full namespace migration** from:

- `synthia/...` → `hexe/...`

There is **no dual namespace compatibility layer** in this phase.

### Goal
Make `hexe` the only active namespace and remove legacy `synthia` topic usage from active runtime behavior.

### This phase SHOULD
- rename MQTT topic root to `hexe`
- update all topic helpers
- update all publishers and subscribers
- update notifications/bootstrap/node/addon/supervisor topic families
- update docs/examples/tests
- remove hardcoded `synthia/...` topic usage from active code

### This phase should NOT
- keep dual publish/subscribe support
- preserve legacy runtime topic compatibility
- leave ambiguous topic-root behavior in active code

---

## [TODO] Task 640: Create Phase 2 full namespace migration document

Create the canonical migration doc for full MQTT/topic namespace cutover to Hexe.

Include:
- purpose
- scope
- affected topic families
- required code/runtime changes
- impacted nodes/addons/integrations
- migration risks
- verification checklist

Deliverables:
- `docs/migration/phase-2-full-namespace-migration-hexe.md`

---

## [TODO] Task 641: Replace configurable legacy topic-root plan with canonical Hexe topic root

Set the active topic root to:

```env
MQTT_TOPIC_ROOT=hexe
````

Remove the need for:

* `MQTT_LEGACY_TOPIC_ROOT`
* `MQTT_ENABLE_LEGACY_COMPAT`

unless they are still temporarily present as dead config pending cleanup.

Deliverables:

* config/settings update
* single-root behavior
* example env docs updated

---

## [TODO] Task 642: Refactor MQTT topic helpers to use `hexe` root only

Replace all active topic helper generation based on `synthia/...` with `hexe/...`.

Ensure helpers generate canonical Hexe topics only.

Examples:

* `hexe/bootstrap/core`
* `hexe/notify/internal/popup`
* `hexe/nodes/{node_id}/...`
* `hexe/addons/{addon_id}/...`

Deliverables:

* topic helper refactor
* no active helper returns `synthia/...`
* tests updated

---

## [TODO] Task 643: Replace hardcoded `synthia/...` topic strings across backend runtime code

Search and replace all active hardcoded MQTT topic strings using `synthia/...`.

Targets include:

* publishers
* subscribers
* notification flows
* bootstrap publication
* supervisor/runtime messaging
* node/addon/service topic generation
* observability/debug helpers

Rules:

* replace active runtime usage
* preserve only archival/historical references where explicitly documented

Deliverables:

* backend runtime cleanup
* reduced hardcoded topic strings
* audit notes for any retained references

---

## [TODO] Task 644: Migrate notification bus topics to Hexe namespace

Rename notification topics to:

* `hexe/notify/internal/popup`
* `hexe/notify/internal/event`
* `hexe/notify/internal/state`
* `hexe/notify/external/{target}`

Update:

* notification publisher
* notification producer
* notification consumer
* notification bridge
* docs/examples/tests

Deliverables:

* notification bus fully migrated
* no active legacy topic usage in notification flows

---

## [TODO] Task 645: Migrate bootstrap publication topic to Hexe namespace

Rename bootstrap publication topic to:

* `hexe/bootstrap/core`

Update:

* bootstrap publisher
* startup reconciliation behavior
* onboarding discovery docs
* tests/examples

Deliverables:

* bootstrap topic migrated
* docs/tests aligned

---

## [TODO] Task 646: Migrate node topic families to Hexe namespace

Rename node-related topic helpers and active usage to:

* `hexe/nodes/{node_id}/...`

Update:

* node onboarding/discovery-related topic references
* node telemetry/messaging helpers
* node docs/examples/tests

Deliverables:

* node topic family migration completed

---

## [TODO] Task 647: Migrate addon topic families to Hexe namespace

Rename addon-related topic helpers and active usage to:

* `hexe/addons/{addon_id}/...`

Update:

* addon messaging helpers
* addon docs/examples/tests
* any store/registry/runtime references that generate MQTT topic strings

Deliverables:

* addon topic family migration completed

---

## [TODO] Task 648: Migrate supervisor/system/runtime topic families to Hexe namespace

Rename active topic usage for:

* supervisor
* runtime
* core
* system
* scheduler
* policy
* telemetry
* services
* bridges
* events
* remote
* import

Deliverables:

* subsystem topic family migration completed
* helpers/docs/tests updated

---

## [TODO] Task 649: Update reserved topic-family definitions and classifiers

Update topic-family classifier logic so Hexe namespace is canonical.

Replace active root handling from `synthia/` to `hexe/`.

Ensure:

* reserved family classification works
* invalid/external classification still works
* scoped helper logic still works
* policy topic matching still works

Deliverables:

* classifier refactor
* tests updated

---

## [TODO] Task 650: Update MQTT authority/runtime docs and examples to Hexe namespace

Update docs so Hexe is the only canonical namespace in active docs.

At minimum update:

* MQTT platform docs
* topic family docs
* notification docs
* bootstrap/onboarding docs where MQTT topics appear

Rules:

* use `hexe/...` in all active examples
* move `synthia/...` references into migration/archive notes only if needed

Deliverables:

* docs updated
* examples aligned

---

## [TODO] Task 651: Update node onboarding/bootstrap documentation for Hexe namespace

Update onboarding docs and examples so all active bootstrap topic references use:

* `hexe/bootstrap/core`

Update any onboarding/migration guidance that still references legacy root as active.

Deliverables:

* node docs updated
* onboarding examples aligned

---

## [TODO] Task 652: Update notification payload examples and HA bridge examples

Update all example payloads and docs blocks that mention old topic names.

Ensure:

* topic examples use Hexe
* branding inside examples uses Hexe AI / Hexe Core where applicable

Deliverables:

* example payload refresh
* doc consistency

---

## [TODO] Task 653: Update tests for Hexe-only namespace behavior

Update automated tests to assume Hexe namespace only.

Remove or rewrite tests that assume:

* `synthia/...` root
* dual compatibility behavior
* legacy topic mirroring

Deliverables:

* MQTT/topic tests updated
* notification/bootstrap/topic-family tests updated

---

## [TODO] Task 654: Add repository-wide audit for remaining active `synthia/...` topic usage

Perform an audit for remaining `synthia/...` references and classify them as:

* blocking active runtime reference
* test fixture
* historical/archive doc
* migration note
* intentional compatibility holdout

Deliverables:

* audit report
* cleanup list for blocking references

---

## [TODO] Task 655: Remove or isolate obsolete dual-namespace planning/config

Clean up any abandoned work related to:

* dual namespace configs
* legacy compat toggles
* dual publish/subscribe helpers
* placeholder migration code that is no longer needed

Deliverables:

* code cleanup
* config cleanup
* reduced migration complexity

---

## [TODO] Task 656: Verify node/addon/runtime clients are aligned to Hexe namespace

Confirm all active internal clients and expected external node/addon integrations are updated to Hexe namespace.

Where needed, document required consumer updates.

Deliverables:

* compatibility verification notes
* required update checklist for connected components

---

## [TODO] Task 657: Add Phase 2 verification pass

Verify:

* all active runtime topic generation uses `hexe/...`
* notification flows work
* bootstrap publication works
* topic classifier works
* docs and examples are updated
* no required active consumer still depends on `synthia/...`

Deliverables:

* Phase 2 verification notes
* unresolved blockers list if any

---

## [TODO] Task 658: Create Phase 2 completion report

Summarize:

* namespace migration completed
* active topic root is now `hexe`
* removed legacy behavior
* remaining non-runtime historical references if any
* readiness for next phase

Deliverables:

* `docs/migration/phase-2-completion-report.md`

```

The next phase after this should be the **internal identifier/code rename cleanup** rather than dual namespace logic.
```

