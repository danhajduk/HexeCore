# Provider Setup

This template includes a provider boundary but no real provider implementation.

Add provider-specific configuration, health checks, APIs, and runtime behavior under:

- `src/node_template/providers/`
- `frontend/src/features/providers/`
